language: shell
dist: trusty
sudo: required

services:
  - docker

before_script:
    - cd docker
    - docker network create OMS
    - sudo docker-compose build
    - sudo docker-compose up -d
    - sudo docker-compose exec alastair ash /usr/src/scripts/bootstrap.sh


script:
    - sudo docker-compose exec alastair mix test
